// Package repositories will contain all driver adapters
// These repositories can be a direct connection to database, communication
// to other service, etc. and must implement a driven port interface
package repositories
